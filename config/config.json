{
  "_comment": "Global settings apply to all chains. Per-chain settings override globals. Set 'null' to use dynamic values or disable a filter.",
  "global": {
    "_comment_mode": "Options: 'testnet' (only testnet chains), 'mainnet' (only mainnet chains), 'all' (everything enabled)",
    "mode": "testnet",
    "_comment_concurrency": "How many wallets run in parallel. Keep low to avoid RPC rate limits.",
    "concurrency": 10,
    "_comment_delay": "[min, max] seconds of random delay between wallets. Reduces detection and API hammering.",
    "delay_between_wallets_sec": [
      5,
      15
    ],
    "_comment_cooldown": "Hours between mint sessions per wallet. 24 = once per day.",
    "cooldown_hours": 24,
    "_comment_cooldown_on_fail": "If true, cooldown is set even if all mints fail. If false, cooldown only set on any success.",
    "cooldown_on_fail": false,
    "_comment_target_mints": "[min, max] random number of NFTs to mint per wallet per session.",
    "target_mints_per_session": [
      1,
      3
    ],
    "_comment_index_cache_hours": "How long to use the cached collection list before refreshing from API.",
    "index_cache_hours": 6,
    "_comment_max_api_pages": "Max pages to fetch from Sweep Haus API per refresh (16 items/page).",
    "max_api_pages": 3
  },
  "chains": {
    "arc_testnet": {
      "_comment": "ARC Testnet — key must match the chain key in rpc.json exactly",
      "enabled": true,
      "_comment_max_price_native": "Max price per NFT in native token. 0 = free only. null = no limit.",
      "max_price_native": 0.005,
      "_comment_gas_price_gwei": "Gas price in Gwei. 0 or null = use on-chain eth_gasPrice dynamically.",
      "gas_price_gwei": 0,
      "_comment_gas_limit": "Hard gas limit per transaction. 280000 is safe for most Sweep Haus claim().",
      "gas_limit": 280000,
      "_comment_gas_multiplier": "Multiplier on top of base gas price for maxFeePerGas.",
      "gas_multiplier": 1.3,
      "_comment_priority_multiplier": "Multiplier on top of base gas price for maxPriorityFeePerGas.",
      "priority_multiplier": 1.1,
      "_comment_gas_buffer_units": "Gas units reserved for balance check buffer. Buffer (wei) = gas_buffer_units x base_gas_price.",
      "gas_buffer_units": 400000,
      "manual_collections": []
    },
    "x1_testnet": {
      "_comment": "X1 EcoChain Testnet — free airdrop farming",
      "enabled": true,
      "_comment_max_price_native": "Max price per NFT in native token. 0 = free only. null = no limit.",
      "max_price_native": 1,
      "_comment_gas_price_gwei": "Gas price in Gwei. 0 or null = use on-chain eth_gasPrice dynamically. Set a number to fix it (e.g. 1 for X1 testnet if chain is slow to respond).",
      "gas_price_gwei": 0,
      "_comment_gas_limit": "Hard gas limit per transaction. 280000 is safe for most Sweep Haus claim().",
      "gas_limit": 280000,
      "_comment_gas_multiplier": "Multiplier on top of base gas price for maxFeePerGas.",
      "gas_multiplier": 1.2,
      "_comment_priority_multiplier": "Multiplier on top of base gas price for maxPriorityFeePerGas.",
      "priority_multiplier": 1.1,
      "_comment_gas_buffer_units": "Gas units reserved for balance check buffer. Buffer (wei) = gas_buffer_units × base_gas_price. Default 350000 is safe for most EVM chains.",
      "gas_buffer_units": 350000,
      "_comment_manual_collections": "Optional. List of manual contracts to bypass Sweep Haus API and mint directly. If configured, API fetching for this chain is bypassed.",
      "manual_collections": []
    }
  }
}